{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "//": "The ' 2.ts' / ' 3.ts' patterns exclude iCloud's duplicate files. This project lives under ~/Documents, which iCloud syncs, and it periodically duplicates generated files inside .next/types with a numbered suffix. Those duplicates then fail typecheck with 'Duplicate identifier' or a missing-file error that looks like a real break and is not.",
  "exclude": ["node_modules", "**/* [0-9].ts", "**/* [0-9].tsx"]
}
